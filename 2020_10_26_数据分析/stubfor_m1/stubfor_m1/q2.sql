/* Q2 is query which returns all the data in the database as a CSV
file which should look like `People.csv`. Indeed, for the very same data it
should return a table that is identical to `People.csv` (up to order, etc.;
header should be identical). */
